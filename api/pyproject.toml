[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "graph-api"
version = "0.1"
description = "Graph Visualiser API (domain model + plugin interfaces)."
dependencies = []

[tool.setuptools]
provides = ["graph"]